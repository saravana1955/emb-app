
<div class="form-container">
  <h1>Employee Form</h1>

  <div class="form-group">
    <label class="control-label col-sm-2" for="name">Name</label>
    <div>
<input type="text" class="form-control"  value={{newName}} oninput={{action (mut newName) value="target.value"}}  placeholder="Enter Your Name Here">
    </div>
  </div>
  <div class="form-group">
    <label class="control-label col-sm-2" for="age">Age</label>
    <div>
      <input type="text" class="form-control" value={{newAge}} oninput={{action (mut newAge) value="target.value"}} placeholder="Enter Your Age Here">
    </div>
  </div>
  <div class="form-group">
    <label class="control-label col-sm-2" for="salary">Salary</label>
    <div>
      <input type="text" class="form-control" value={{newSalary}} oninput={{action (mut newSalary) value="target.value"}} placeholder="Enter Your Salary Here">
    </div>
  </div>

<button onclick={{action "onAdd"}} class="addBtn">Add</button>
</div>
<div >
  <h1>Employees List</h1>
<table id="emp-details" class="form-container">
  <thead>
    <tr>
      <th>Name</th>
      <th>Age</th>
      <th>Salary</th>
    
    </tr>
  </thead>
  <tbody>
    {{#each employees as |emp|}}
    <tr>
      <td>{{emp.name}}</td>
      <td>{{emp.age}}</td>
      <td>{{emp.salary}}</td>



    </tr>
    {{/each}}
  </tbody>
</table>
</div>

<h2>Set timeout in 3s</h2>
<p>{{fullName}}</p>